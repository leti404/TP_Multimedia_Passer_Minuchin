import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  icon: {
    width: 28,
    height: 28,
    marginRight: 10,
    tintColor: '#0a7ea4', // se puede reemplazar con theme
  },
});
